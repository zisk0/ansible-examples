---
  - hosts: almacenes

    # vars_files:
      # OJO: Esto es relativo a donde se ejecute el playbook.

      #- hosts/{{inventory_hostname}}/vars
      #- host_vars/{{inventory_hostname}}/mysql-slave.yml

    roles: 
      - almacen